export class ReactionDiffusion {

    constructor(renderer) {
        this.renderer = renderer;

        // create pipeline and bind group layouts

        // create all buffers, storage texture, ...
    }

    async init() {
        // create pipelines

        // create bind groups
    }

    compute(computePassEncoder) {

    }
}
